import amin_machine_spec;

class Amin (Eo.Base)
{
   data: Amin_Data;
   methods {
      /* @Starts the amin machine processing the current XML profile */
      parse { 
        params {
            @in profile: char*;
        }
	return: own(Eo*);
      }
   }
   implements {
   }
}
