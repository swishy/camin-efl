class Amin.Machine.Spec.Data (Eo.Base)
{
   data: Spec_Data;
   properties {
      filters {
         set { /*@ This sets the filters for the current spec */
         }
         get { /*@ This gets the filters for the current machine document if set */
         }
         values {
            Eina_Hash *filters; /*@ The filters for the current machine document */
         }
      }
   }
   methods {
   }
   implements {
   }
}
